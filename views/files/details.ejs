<%- include('../partials/head.ejs') %>

    <body class="min-vh-100 d-flex flex-column justify-content-between m-0">
        <%- include('../partials/header.ejs') %>
            <div class="flex-grow-1 p-3">
                <a class="btn btn-outline-info mb-3" href="/folders/<%= folderId %>"><i
                        class="bi bi-arrow-left-square"></i></a>
                <h1>💾
                    <%= title %>
                </h1>
                <p>
                    Size: <%= (file.size / 1024).toFixed(2) %> KB
                </p>
                <div class="d-flex gap-2">
                    <a class="btn btn-primary" href="/files/<%= file.id %>/download">Download</a>
                    <form action="/files/<%= file.id %>" method="post">
                        <input type="hidden" name="_method" value="DELETE">
                        <input type="hidden" name="folderId" value="<%= folderId %>">
                        <button class="btn btn-danger" type="submit"
                            onclick="return confirm('Are you sure')">Delete</button>
                    </form>
                </div>
            </div>
            <%- include('../partials/footer.ejs') %>
    </body>

    </html>